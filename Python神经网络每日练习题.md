# Python → 神经网络 · 每日练习题（Day 1–21）

> 配套：`Python神经网络手把手教程.md`（Day 1–21）
> 用法：**先自己写出代码跑通**（代码全靠你自己打，本文件不给答案，题目里也不出现任何函数名——只告诉你要实现什么功能，具体用哪个函数由你自己回想）。做完后如果想核对，再看同目录的 `Python神经网络每日练习题答案.md`。
> 原则：每天小题**覆盖当天教程教到的全部知识点**，不提前使用没学过的函数。
> 约定：默认已导入 numpy 与 matplotlib 的 pyplot。

---

## 第 1 天 练习题：创建数组（覆盖：几种数组创建方式、形状/类型/维度数、改变形状、两个常见坑）

**题目**
1. 从嵌套列表 `[[1,2],[3,4],[5,6]]` 创建一个 3×2 矩阵 `a`，打印它的「形状」「元素类型」「维度数」。
2. 生成 0 到 11 的连续整数数组，再把它改成 3 行 4 列并打印。
3. 分别创建并打印：全 0 的 2×3 数组、全 1 的 2×2 整数数组、全为 7 的 2×3 数组。
4. **坑（先想再运行）**：想创建二维数组时，如果写成 `array([1,2],[3,4])`（每行一个列表、却少了最外层括号）会报什么错？正确写法是什么？
5. **坑**：连续整数 0~9（共 10 个数）想改成 3 行 4 列会报错吗？为什么？（看元素总数是否匹配）

**提示**：创建数组的几种方式当天都教过；改变形状时新形状的元素总数必须等于原数组。

---

## 第 2 天 练习题：索引、切片与条件筛选（覆盖：取单个元素、切片、按条件筛选、掩码、元素个数）

**题目**
1. 先生成一个 3 行 4 列的矩阵 `m`（把 0~11 排进去）。取出「第 1 行第 2 列」的元素，值是多少？（行、列都从 0 开始数）
2. 用切片取出「前 2 行、第 1~2 列」的子矩阵。
3. 取出 `m` 中所有大于 6 的元素，并打印一共有多少个。
4. 把 `m` 中大于 6 的位置标成 1、其余标成 0，做成一个 0/1 掩码并打印。

**提示**：取单个元素用「行, 列」；切片左闭右开；按条件筛选会返回一维结果。

---

## 第 3 天 练习题：广播机制（覆盖：对齐规则、加到每列、加到每行、形状不匹配报错）

**题目**
1. 创建一个 3 行 4 列、值全为 10 的矩阵 `M`。再造一个 3 行 1 列的列向量 `[1,2,3]`，把它加到 `M` 上，描述每一列分别多了什么。
2. 再造一个 1 行 4 列的行向量 `[1,2,3,4]`，加到 `M` 上，描述每一行分别多了什么。
3. 手算并用代码验证：一个 3×1 的列向量 `[[1],[2],[3]]` 与一个 1×3 的行向量 `[[10,20,30]]` 相加，结果是什么形状、什么值？
4. **报错验证**：直接把一个长度为 3 的一维数组加到 `M`（形状 3×4）上，会报什么错？用异常处理把它捕获并打印错误信息。

**提示**：广播从最后一个维度开始往前对齐；长度 3 的一维数组加不到 3×4 上，要先改成 3 行 1 列或 1 行 3 列的形状。

---

## 第 4 天 练习题：矩阵乘 vs 对应位置相乘（覆盖：矩阵乘、逐元素乘、手算验证、转置）

**题目**
1. `A=[[1,2],[3,4]]`，`B=[[5,6],[7,8]]`。分别用「矩阵乘法」和「对应位置相乘」两种方式计算并打印结果。
2. 手算验证矩阵乘法结果第 0 行第 0 列的值（1×5 + 2×7 = 19）。
3. 对 `A` 做转置并打印，再对「转置后的矩阵」再做一次转置，验证它等于原来的 `A`。

**提示**：矩阵乘要求「前者的列数 = 后者的行数」；逐元素乘是位置一一对应相乘；转置交换行和列。

---

## 第 5 天 练习题：随机数与种子（覆盖：正态随机、固定种子、均值/标准差、均匀随机、不设种子）

**题目**
1. 固定随机种子为 0，生成 10000 个「标准正态分布」样本，打印它们的均值和标准差，它们应接近什么数？
2. 再次固定种子为 0 生成同样的 10000 个，验证两次生成的数组完全一致。
3. 生成 5 个「0~1 均匀分布」的随机数并打印，观察它和正态分布在取值范围上的差异。
4. **不设种子**，连续生成两组各 3 个标准正态分布随机数，两次结果一样吗？打印说明。

**提示**：标准正态的均值≈0、标准差≈1；均匀分布取值在 0~1，正态可取负数且分布更宽；同一种子→同结果，不设种子→每次都不同。

---

## 第 6 天 练习题：画图（覆盖：折线、散点、名称标签、图例、坐标轴/标题、显示）

**题目**
1. 生成 -3 到 3 共 100 个等间距的 x，画出 `y = x²` 的折线，并给这条线起个名字。
2. 在同一张图里，随机取 30 个点（x 在 -3~3 随机取、y 取 x² 加上随机噪声）画成散点，也给它起个名字。
3. 给图加上横轴标签、纵轴标签、标题「抛物线」、图例，并把图显示出来。

**提示**：同一张图连续画折线/散点就能叠加；图例依赖每条线/点都设了名字。

---

## 第 7 天 练习题：第 1 周综合小测（覆盖：Day 1–6 全部知识点）

**题目**（尽量不翻资料）
1. 生成 0~8 的连续整数并改成 3×3，打印它的形状、元素类型、维度数。
2. 取出它的「第 2 行」以及「前两行、第 2~3 列」这两个子矩阵。
3. 取出所有大于 4 的元素，并做一个「是否大于 4」的 0/1 掩码。
4. 写一个 3×3 矩阵，让向量 `[1,2,3]` 通过广播加到它的每一列（先改成正确的形状）。
5. 定义单位阵 `[[1,0],[0,1]]` 和矩阵 `[[2,3],[4,5]]`，分别用矩阵乘、逐元素乘两种算法计算，并对后者做转置。
6. 固定种子 0 后生成 1000 个正态样本，打印均值；再把种子设回 0 生成一次，证明两次相同。

---

## 第 8 天 练习题：函数封装（覆盖：默认参数、接收任意个参数、封装成 make_matrix、可变默认参数坑）

**题目**
1. 写一个函数 `make_matrix(rows, cols, seed=0)`：在函数内部先固定随机种子，再返回一个随机矩阵。分别调用不传种子和传种子 1，打印说明两者是否相同。
2. 写一个函数 `total`，它能接收任意多个位置参数并返回它们的和，然后调用它传入 `1, 2, 3, 4`。
3. **坑验证**：写一个函数，默认参数用一个空列表，函数体里往这个列表追加传入的元素再把它返回。连续调用 `f(1)`、`f(2)` 会发生什么？说明为什么默认参数用可变对象很危险。

**提示**：默认参数可以省略不传；接收任意个位置参数有专门的写法；默认参数只在函数定义时创建一次，可变对象会跨多次调用共享。

---

## 第 9 天 练习题：Linear 类（上）（覆盖：class、初始化、self、y=x·W+b、小权重初始化、零偏置、forward）

**题目**
1. 从零写一个 `Linear` 类：初始化时接收输入维度和输出维度，以及可选的随机种子；用「均值为 0 的小随机数乘以 0.1」来设置权重，用「全 0 向量」来设置偏置；再写一个 `forward` 方法，返回输入经过「矩阵乘权重 + 偏置」后的结果。
2. 创建 `layer = Linear(2, 3)`，打印权重的形状（应 `(2,3)`）、偏置（3 个 0），并解释为什么权重要乘 0.1。
3. `x = [[1.0, 2.0]]`（注意写成二维），用 `layer.forward(x)` 算一遍，确认输出形状是 `(1, 3)`。

**提示**：输入维度对应 x 的特征数、输出维度对应结果的维度；x 必须是二维的（哪怕只有 1 个样本也要写成 `[[...]]`）。类看不懂先翻 `Python二级衔接补充.md` 第 1 节。

---

## 第 10 天 练习题：让实例可调用（覆盖：特殊方法 __call__、layer(x)==forward(x)、必须 return）

**题目**
1. 在第 9 天的 `Linear` 类里加一个特殊方法，使「实例本身能像函数一样被调用」，并且调用时等价于 `forward`。
2. 创建 `layer = Linear(1, 1)`，手动把权重设成二维的 `[[2.0]]`、偏置设成 `[1.0]`，分别用 `layer.forward(...)` 和「直接把实例当函数调用」两种方式验证结果一致（应为 `[[7.]]`）。
3. **坑验证**：写这个特殊方法时如果忘记写 `return`（即只调用了 forward 却没返回），再调用实例会得到什么？说明原因。

**提示**：这个特殊方法让 `对象(参数)` 等价于 `对象.forward(参数)`；忘了 return，调用结果就是「空」。

---

## 第 11 天 练习题：异常处理与文件读写（覆盖：try/except、数组存盘/读盘、文件不存在异常、存的是复制）

**题目**
1. 生成一个 3 行 4 列的矩阵，把它存成 `m.npy` 文件，再读回来，验证读回的数组与原数组完全一致。
2. 用异常处理去读一个不存在的文件 `no_such.npy`，捕获后打印「文件不存在，请检查路径」。
3. **验证存的是「复制」**：保存之后修改原数组的某个元素，再读回文件，打印该元素看变没变，说明存盘存的是当时的值还是引用。

**提示**：读盘时文件不存在会抛「文件未找到」类异常；存盘写的是数组当时的快照，之后改原数组不会影响已存的文件。

---

## 第 12 天 练习题：Jupyter 操作（覆盖：新建 notebook、Markdown 单元、代码单元、画图、保存）

**题目**：新建一个 Jupyter Notebook，按顺序做：
1. 第一个单元设为 **Markdown**，写 `# 第12天练习：Jupyter 试水`，运行它（应渲染成标题）。
2. 新建 **代码** 单元，导入 numpy 和 pyplot，画出 `x` 在 0~10 取 50 个点的正弦曲线并显示。
3. 把它保存为 `day12_practice.ipynb`。

**提示**：代码单元顶部加一行让图直接显示的魔法命令；变量在所有单元间共享，改了要重跑相关单元。

---

## 第 13 天 练习题：距离与相似度（覆盖：欧氏距离、余弦相似度、向量长度、方向无关长度）

**题目**
1. 写 `euclidean(a, b)` 返回 a 与 b 的欧氏距离；写 `cosine_sim(a, b)` 返回 a 与 b 的余弦相似度（定义为「点积 ÷ 两者长度的乘积」）。
2. `a = [1,0,0]`，`b = [0,1,0]`：两者的欧氏距离和余弦相似度各是多少？
3. 把 `b` 换成 `[2,0,0]`（与 a 同向但长度不同）：余弦变不变？欧氏距离变不变？
4. `a` 与自身的余弦应等于 1，验证一下。

**提示**：正交向量余弦=0；同向向量余弦=1 且不受长度影响；欧氏距离会随长度变化。

---

## 第 14 天 练习题：第 2 周综合自测（覆盖：Day 8–13 全部知识点）

**题目**（尽量不翻资料）
1. 写 `make_matrix(rows, cols, seed=0)`（默认参数 + 封装），调用两次不同种子验证结果不同。
2. 写 `total` 接收任意多个参数并返回它们的和，调用 `total(2, 4, 6)`。
3. 从零写 `Linear` 类（含初始化 / forward / 可调用），输入 3 维、输出 2 维，创建实例后 `layer([[1.0, 2.0, 3.0]])` 跑通。
4. 存盘保存「0~9 连续整数」生成的数组，再读回验证一致；另用异常处理去读一个不存在的文件。
5. 写 `euclidean` 和 `cosine_sim`，算 `a=[1,2,3]` 与 `b=[4,5,6]` 的欧氏距离和余弦相似度。
6. 为什么要给随机数设种子？

---

## 第 15 天 练习题：向量点积（覆盖：点积代数=几何、交换律、余弦比较方向、允许浮点误差的相等判断、点积受长度影响）

**题目**
1. `a=[1.,2.]`, `b=[3.,4.]`。算 a 与 b 的点积；再按「a 的长度 × b 的长度 × 余弦相似度」重建出几何值，验证两者相等（允许极小浮点误差）。
2. 验证「a 与 b 的点积」等于「b 与 a 的点积」（允许极小误差）。
3. 给定 `a=[1,2]`, `b=[3,4]`, `c=[-2,1]`，分别算 cosine_sim(a,b) 与 cosine_sim(a,c)，说明谁与 a 同向、谁与 a 正交（cosine_sim 自己定义）。
4. **坑**：`d=[100,200]`（d 是 a 的 100 倍）。算 a 与 d 的点积和 cosine_sim(a,d)，说明「比较方向要用余弦而非裸点积」。

**提示**：余弦相似度 = 点积 ÷（两向量长度乘积）；正交向量余弦=0。

---

## 第 16 天 练习题：矩阵变换（覆盖：M·p 旋转、行/列向量形状、画变换前后、等比例坐标轴）

**题目**
1. `M=[[0.,-1.],[1.,0.]]`, `p=[1.,0.]`。用矩阵乘把 M 作用到点 p 上，算出变换后的位置并打印，说明转到了哪里。
2. 把 M 作用到单位正方形的四个角 `corners=[[0.,1.,1.,0.],[0.,0.,1.,1.]]` 上，打印变换后的坐标。
3. 画图：变换前和变换后各画一条线，坐标轴设成等比例，加图例并显示。
4. **坑**：若把点写成一维 `[1.,0.]`（形状 (2,)），直接拿 M 去乘它会怎样？说明为什么要把点写成行/列向量形式来匹配乘法维度。

**提示**：点写成行向量时，可以用「p 乘以 M 的转置」来算；画图必须让坐标轴等比例才不变形。

---

## 第 17 天 练习题：数值导数（覆盖：单边差分、与解析解对比、不同步长的误差、f=x³ 验证、中心差分更准）

**题目**
1. 写 `num_deriv(f, x, h=1e-5)` 用单边差分 `(f(x+h)-f(x))/h`，验证 f(x)=x² 在 x=3 处导数≈6。
2. 写 f(x)=x³，验证它在 x=2 处的数值导数≈12（解析解 3x²=12）。
3. 分别试 h=1e-1, 1e-3, 1e-5, 1e-7 打印各自的数值导数，说明 h 太小误差反而变大。
4. **坑**：用单边差分与中心差分各算一次 x² 在 x=3 处的导数，哪个更接近真值 6？

**提示**：单边差分误差随 h 线性下降、中心差分随 h 平方下降；x² 的解析导数是 2x。

---

## 第 18 天 练习题：数值梯度（覆盖：偏导、中心差分、梯度向量、在(1,2)验证[2,4]、向量近似相等判断、梯度=上升方向）

**题目**
1. 写 `num_grad(f, xy)` 用中心差分，验证 f(x,y)=x²+y² 在点 (1,2) 处梯度≈[2,4]。
2. 验证 f(x,y)=x²+2y² 在 (1,1) 处梯度≈[2,4]。
3. 用「允许极小误差的向量相等判断」比较数值梯度与解析梯度。
4. **坑（方向验证）**：在 (1,2) 处沿梯度走一小步 `p2=(1,2)+0.1*[2,4]`，比较 f(p2) 与 f(1,2) 谁更大？说明梯度指向上升方向。

**提示**：梯度是 `[对 x 的偏导, 对 y 的偏导]`；改坐标算偏导时记得复制一份避免改动原数组。

---

## 第 19 天 练习题：链式法则（覆盖：复合、数值导数验证、f'(g(x))·g'(x)、f' 在 g(x) 处求值）

**题目**
1. `g=x²`, `f=u+1`，写 `h=f(g(x))`，用中心差分数值导数验证 h'(3)≈6。
2. 链式法则解析：f'(u)=1, g'(x)=2x → 1·2·3=6，与数值结果对比。
3. 换一组：`g=sin(x)`, `f=u³`，验证 h(x)=sin(x)³ 在 x=0 处导数≈0。
4. **坑**：本题 f'(u)=3u²，要在 g(0)=0 处算 f'(g(0))，而不是 f'(0)（虽然这里都等于 0）。写出「链式法则里 f' 必须在 g(x) 处求值」这句话并对照代码。

**提示**：链式法则是 `h'(x)=f'(g(x))·g'(x)`；本题直接用正弦函数来构造。

---

## 第 20 天 练习题：梯度下降（覆盖：更新规则、学习率、轨迹图、大学习率发散、起点不敏感）

**题目**
1. 写梯度下降求 f(x)=x²−4x 的最小值，学习率=0.1，起点 x=0，迭代 30 步，打印最终 x 与 f(x)。
2. 画出下降轨迹（把每一步的 x 存起来画成曲线，轨迹点用带圈的标记）。
3. 把学习率改成 1.5，打印最终 x，说明发生了什么（发散/震荡）。
4. **坑**：起点改成 x=10、学习率=0.1，最终还能收敛到 2 吗？验证梯度下降对起点不敏感（凸函数）。

**提示**：每步更新 `x = x - 学习率 × 导数`；x²−4x 的导数是 2x−4；用列表记录每一步的 x 来画轨迹。

---

## 第 21 天 练习题：第 3 周综合自测（覆盖：Day 15–20 全部知识点）

**题目**（尽量不翻资料）
1. 验证「a 与 b 的点积 = |a||b|cosθ」且「a·b = b·a」（允许极小误差）。
2. 旋转矩阵 `[[0,−1],[1,0]]` 把 (1,0) 转到哪里？画出来。
3. 数值导数验证 f(x)=x³ 在 x=2 处≈12。
4. 数值梯度验证 f(x,y)=x²+2y² 在 (1,1) 处≈[2,4]。
5. 链式法则验证 h=f∘g（g=x², f=u+1）在 x=3 处 h'≈6。
6. 从零用梯度下降求 f(x)=x²−4x 的最小值并画轨迹。

---

## 第 22 天 练习题：损失函数（覆盖：MSE 公式、形状一致、预测越准损失越小、长度不一致报错）

**题目**
1. 写一个 `mse(y_pred, y_true)` 函数，返回「预测值与真实值之差的平方的平均值」。
2. `y_true=[1.,2.,3.]`，`y_pred=[1.1,1.9,3.2]`，算一下损失是多少？
3. 把 `y_pred` 换成和 `y_true` 完全一样，损失应是多少？
4. **坑**：如果 `y_pred` 和 `y_true` 长度不一样，会发生什么（试一下并说明）？

**提示**：损失衡量"预测有多差"，越小越好；两个数组长度必须一致才能相减求平均。

---

## 第 23 天 练习题：激活函数（覆盖：sigmoid、tanh、ReLU、为什么需要非线性、三曲线对比）

**题目**
1. 写 `sigmoid(x)`，返回 `1/(1+e⁻ˣ)`，并验证：sigmoid(0)=0.5、输入很大正数时结果接近 1、输入很大负数时结果接近 0。
2. 写 `tanh(x)` 和 `relu(x)`（relu 即"取 0 与 x 中较大的那个"）。
3. 生成 -6 到 6 共 100 个等间距点，把三个函数的曲线分别画出来（三个子图并排）。
4. **坑**：如果把 sigmoid 写成 `1/(1+eˣ)`（指数里是 +x 而非 −x），当 x 是很大的正数时会怎样？为什么教程里用 `−x` 形式？

**提示**：激活函数引入非线性，否则多层叠加仍是一条直线；ReLU 用"逐元素取大"而不是乘法。

---

## 第 24 天 练习题：sigmoid 求导（覆盖：σ′=σ(1−σ)、数值验证、5 个点）

**题目**
1. 写 `sigmoid_prime(x)` 返回 `sigmoid(x) × (1 − sigmoid(x))`。
2. 在 x = −2, −1, 0, 1, 2 五个点上，分别用「解析公式」和「数值差分」两种方法算导数，对比两者是否一致（允许极小误差）。
3. **坑**：x=0 时 σ′(0) 应等于多少？（用公式算出来）

**提示**：数值差分用中心差分；σ′ 有个省事性质——只要算出了 sigmoid 就无需再算指数。

---

## 第 25 天 练习题：概率基础（覆盖：均值/方差、正态分布、直方图）

**题目**
1. 生成 10000 个标准正态分布随机数，打印它们的均值和方差，应分别接近什么数？
2. 把这 10000 个数画成直方图（纵轴用「概率密度」模式），再叠一条标准正态的理论曲线。
3. **坑**：直方图默认纵轴是「计数」，想和理论密度曲线对比时该把纵轴改成什么模式？

**提示**：均值≈0、方差≈1；纵轴设为概率密度后，曲线下面积总和为 1，便于叠加理论曲线。

---

## 第 26 天 练习题：交叉熵（覆盖：二元交叉熵、预测越准损失越小、防止 log(0)）

**题目**
1. 写 `binary_cross_entropy(p, y)`，返回 `−[y·log(p) + (1−y)·log(1−p)]`。
2. 固定 y=1，分别算 p=0.9、p=0.5、p=0.1 时的损失，哪个最小、哪个最大？
3. **坑**：如果 p 直接等于 0 或 1 会发生什么？实际训练里通常怎么处理（把 p 限制在一个很小的正区间到接近 1 之间）？

**提示**：y=1 时 p 越接近 1 损失越小、p 接近 0 损失爆炸；直接对 0 取对数会得到负无穷，训练会出 nan。

---

## 第 27 天 练习题：第 4 周综合自测（覆盖：Day 22–26 全部知识点）

**题目**（尽量不翻资料）
1. 手推 f(x)=x³ 在 x=2 处的导数（解析 3x²=12），并用数值导数代码验证。
2. 实现 `tanh` 的数值导数（用中心差分），验证 tanh'(0)=1。
3. 用一句话解释：学习率太大 / 太小，训练分别会发生什么？

---

## 第 28 天 练习题：阶段复盘

**题目**：不写代码，写一段约 200 字总结：「训练一个模型需要哪些零件？」（提示：从「数据 / 模型 / 损失 / 梯度 / 更新规则」五个角色各自扮演什么来展开）

---

---

## 第 5 周 练习题（覆盖：Day 29–35 全部知识点）

### 第 29 天 练习题：认识机器学习数据与数据集
1. 用教程里介绍的机器学习库，加载它自带的鸢尾花示例数据集，取出特征矩阵和标签，然后查看特征矩阵的形状、打印前 5 行特征、打印前 5 个标签，并打印三个品种的名称。
2. 文字题：用自己的话说明"监督学习"和"无监督学习"的区别，各举一个生活里的例子。

### 第 30 天 练习题：生成线性数据
1. 生成 100 个在 0 到 10 之间均匀分布的 x 点（固定随机种子为 0）；按真实规律 y = 2x + 1 算出 y，再加上从标准正态分布采样的随机噪声（噪声幅度自己定）；最后把样本画成散点图。
2. 文字题：为什么要在数据里加噪声？如果完全不加噪声（y 严格等于 2x+1），训练会有什么不同？

### 第 31 天 练习题：手写梯度下降训练线性回归
1. 把参数 w 和 b 都初始化为 0，用学习率 0.01 做 100 轮梯度下降训练（损失用均方误差），每 20 轮打印一次 w、b 和 loss；训练结束后检查学到的 w、b 是否接近 2 和 1。
2. 文字题：为什么"w 的梯度"里要乘上 x，而"b 的梯度"不用？从"哪个参数影响输出、梯度就乘上它对输出的贡献"这个直觉解释。

### 第 32 天 练习题：学习率的影响
1. 写一个训练函数，接受一个学习率参数并在内部做 100 轮梯度下降，返回每一轮的 loss；用三个不同的学习率（一个很小、一个适中、一个很大）各训练一次，把三条 loss 曲线画在同一张图，纵轴用对数刻度。
2. 观察并描述：学习率太小时曲线怎样？太大时曲线又怎样？

### 第 33 天 练习题：早停
1. 在第 31 天的训练循环里加入"早停"：记录上一轮的 loss，当相邻两次 loss 的差小于 1e-6 就停止训练，并打印实际用了多少轮。注意把初始 loss 设成一个很大的数，保证第一轮不会误判为已收敛。

### 第 34 天 练习题：向量化
1. 分别用两种方式实现 100 轮训练并比较速度：方式一是逐个样本用 for 循环更新参数；方式二是用整批数组运算一次算完所有样本的误差和梯度。用计时工具（Jupyter 的计时命令或 time 模块）对比两者耗时。
2. 把偏置并入参数矩阵：给特征加一列常量 1，用一次矩阵乘法同时得到 wx 和 +b，验证结果和分开算一致。

### 第 35 天 练习题：复盘日（画拟合直线）
1. 用学到的 w、b 在原始散点图上画一条红色拟合直线，直线横坐标范围只覆盖数据的 x 区间，并打印学到的 w、b（应接近 2 和 1）。
2. 合上资料，默写训练一个模型需要的五个零件（数据 / 模型 / 损失 / 梯度 / 更新规则），并各用一句话说明它们的作用。

---

---

## 第 6 周 练习题（覆盖：Day 36–42 全部知识点）

### 第 36 天 练习题：过拟合与训练/测试集
1. 生成 20 个在 -3 到 3 之间均匀分布的 x 和对应的 y（y 近似等于 0.5x 再加随机噪声）；把样本按 8:2 分成训练集和测试集，打印两边的样本数量。
2. 用高次多项式（比如 9 次）去拟合训练样本，把训练点和高次曲线画在同一张图，观察曲线在训练点之间的扭曲振荡。
3. 文字题：为什么要分成训练集和测试集？测试集在训练时能不能看？

### 第 37 天 练习题：逻辑回归前向
1. 生成两类二维点，每类 50 个，两类中心分别在右上方和左下方；把所有点按行拼成特征矩阵，并造出对应的 0/1 标签。
2. 实现 sigmoid 函数（把任意实数压到 0~1）；用一组随便给的权重和偏置算每个点的概率，再用 0.5 阈值把概率转成 0/1 类别，打印前几个概率和预测类别，以及整体准确率。
3. 文字题：逻辑回归的"前向"分哪三步？

### 第 38 天 练习题：交叉熵训练逻辑回归
1. 用交叉熵损失加梯度下降，把第 37 天的随机权重训练成能分开两类的参数（训练约 2000 轮，每若干轮打印一次 loss）；训练后在网格上算每个点的概率，画出决策边界（0.5 那条分界线）和两类散点。
2. 文字题：逻辑回归和线性回归的梯度，区别只在哪？

### 第 39 天 练习题：准确率与混淆矩阵
1. 用训练好的参数对全部样本做预测，逐个比对预测类别和真实标签，统计出混淆矩阵四个格子（真正 / 假正 / 假负 / 真负）的数量，并算出整体准确率。
2. 文字题：混淆矩阵里 FP 代表"实际是__、预测是__"的哪种错误？

### 第 40 天 练习题：特征标准化
1. 构造两个量纲相差很大的特征（一个很小、一个很大），按线性规律生成 y；分别在不做标准化、做 z-score 标准化（减均值除标准差）两种情况下训练，比较同样轮数下最终 loss 的大小。
2. 文字题：标准化主要解决了什么问题？

### 第 41 天 练习题：综合实战 pipeline
1. 把前面所有步骤装进一个函数，内部依次完成：生成两类数据 → 打乱并按 8:2 划分 → 用训练集的统计量做标准化（测试集用同一套统计量）→ 交叉熵梯度下降训练 → 在测试集上算准确率 → 画决策边界图；最后返回训练好的参数和测试准确率。
2. 文字题：标准化时，测试集的均值和标准差应该用谁算出来的？为什么？

### 第 42 天 练习题：阶段复盘
1. 合上资料，默写"训练一个模型需要的五个零件"，并各用一句话说明作用。
2. 文字题：线性回归和逻辑回归，在"模型、损失、梯度"三方面各有什么相同与不同？
3. 写 200 字阶段总结：过拟合是什么、标准化为什么能加速训练。

---

---

## 第 7 周 练习题：神经网络核心·前向传播（Day 43–49）

> 题目只描述「要实现什么功能」，函数名由你自己回想；代码全靠自己打，本文件不含答案。做完想核对再看 `Python神经网络每日练习题答案.md`。

### Day 43 练习题：感知机
1. 用权重 [0.5, 0.5]、偏置 −0.7 实现一个 AND 门：对 (0,0)、(0,1)、(1,0)、(1,1) 四个输入，分别算出的结果应为 0、0、0、1。
2. 把同一套逻辑改成 OR 门（权重 [0.5, 0.5]、偏置 −0.2），验证四个输入的结果应为 0、1、1、1。
3. 把同一套逻辑改成单输入的 NOT 门（权重 [−0.5]、偏置 0.2），验证 0→1、1→0。

### Day 44 练习题：单层感知机的局限（XOR 不可分）
1. 准备好 XOR 的 4 个输入样本和对应的 0/1 标签。
2. 用「单层模型 + 概率变换 + 交叉熵损失 + 梯度下降」反复训练足够多轮。
3. 观察并描述：训练结束后 4 个样本的预测概率最终落在什么范围？损失还能继续下降吗？
4. 多换几次随机初始值重复训练，看结论是否一致。

### Day 45 练习题：多层感知机结构与参数计数
1. 用数组定义 2−2−1 网络的四组参数：输入到隐藏的 2×2 矩阵、隐藏层的 2 个偏置、隐藏到输出的 2×1 矩阵、输出层的 1 个偏置。
2. 数出参数总数，确认是 9。
3. 在纸上画出这个网络结构图（2 个输入圆、2 个隐藏圆、1 个输出圆，连线表示权重，各圆旁标注偏置）。

### Day 46 练习题：前向传播（单个样本）
1. 取一个 XOR 样本 (0,1)，定义随机的 2×2、2、2×1、1 四组参数（初始值要小）。
2. 依次算出：线性变换到隐藏层、隐藏层非线性变换、线性变换到输出层、输出层概率变换，得到这个样本的最终输出概率。
3. 记录每一步结果的形状。

### Day 47 练习题：前向传播（批量矩阵形式）
1. 把 4 个 XOR 样本竖着叠成一个矩阵。
2. 用批量方式一次性算出 4 个样本的前向结果。
3. 记录输入矩阵和各中间结果的形状，确认输出是 4 行 1 列。

### Day 48 练习题：把网络封装成类
1. 用类把这套网络封装起来：初始化时存好四组参数（初始值要小），并提供一个「前向计算」方法，接收输入、返回输出概率。
2. 用这个类对 4 个 XOR 样本做一次前向，得到 4 个概率。
3. 给这个类再加一个「信息」方法，打印四组参数的形状和参数总数（应为 9），调用验证。

### Day 49 复盘日 练习题
1. 不看资料，手画 2−2−1 前向计算图，标出每个中间量的形状（输入 → 隐藏层线性 → 隐藏层激活 → 输出层线性 → 输出概率）。
2. 文字描述每一步形状怎么从 (4,2) 变到 (4,1)。

---

---

## 第 8 周 练习题：反向传播（Day 50–56）

> 题目只描述「要实现什么功能」，函数名由你自己回想；代码全靠自己打，本文件不含答案。做完想核对再看 `Python神经网络每日练习题答案.md`。

### Day 50 练习题：计算图与链式法则
1. 定义函数 f(x)=(x+1)²，写出它的解析导数（手推），并写一个用中心差分算数值导数的函数。
2. 取 x=3，对比解析导数和数值导数是否一致。
3. 把 f 换成 3x²+2x+1，手推导数再用数值导数验证两者一致。

### Day 51 练习题：输出层误差信号
1. 取一个真实标签和一组随机的 z，过概率变换得到预测值，算出输出层的误差信号（预测减真实）。
2. 把真实标签改成另一个值，再算一次，观察误差信号符号的变化。
3. 用自己的话描述"误差信号 δ 是什么、它从哪开始往后传"。

### Day 52 练习题：误差逐层回传
1. 给定一组隐藏到输出的权重、一个输出层误差信号、一组隐藏层线性值，算出"误差倒流回隐藏层"的结果（权重转置乘误差，再逐元素乘隐藏层激活的导数）。
2. 把误差信号换成批量形式（多行），确认结果的行数和隐藏层一致。

### Day 53 练习题：算出所有梯度
1. 在前向的基础上，写一个"反向"方法：先算出输出层误差信号，再依次算出隐藏层误差、以及四个参数各自的梯度。
2. 检查每个梯度的形状是否和对应参数一致。
3. 确认梯度不是全 0。

### Day 54 练习题：梯度校验
1. 定义这个网络的损失函数（交叉熵）。
2. 用"对某个权重元素加/减一个极小值、重新算损失、作差除以 2 倍极小值"的方式，得到该权重的数值梯度。
3. 把它和前一天 backward 算出的同一个梯度对比，确认两者接近。

### Day 55 练习题：完整训练循环
1. 把"前向 → 算损失 → 反向算梯度 → 用学习率更新参数"串成一个训练循环，对 XOR 数据反复多轮。
2. 每若干轮记录一次损失，训练结束后画出损失随轮数下降的曲线。
3. 观察最终预测是否接近 XOR 真值。

### Day 56 里程碑 练习题：XOR 训练成功
1. 加大训练轮数，调整学习率，让 4 个 XOR 输入全部分类正确、损失低于 0.01。
2. 把最终预测概率按 0.5 阈值转成 0/1 类别，和真实标签逐一对齐，确认全部一致。

---

> 📌 **Day 1–56 练习题到此。** 每天小题覆盖当天教程全部知识点，题目只描述功能、不含函数名和代码答案——函数由你自己回想、代码全靠你自己打。做完想核对再看 `Python神经网络每日练习题答案.md`。
> 告诉我「继续写第 9 周」，我就出下一批（从玩具到真实数据：隐藏层宽度、ReLU vs sigmoid、初始化、mini-batch、训练 MNIST）。

---

# 第 9 周练习题（Day 57–63）：从玩具到真实数据

> 规则不变：题目只说「要实现什么功能」，函数名你自己回想；每天多小题覆盖当天全部知识点；答案代码不在这里，练完去 `Python神经网络每日练习题答案.md` 核对。

## 第 57 天 练习题：隐藏层宽度的影响
1. 把你之前能学会 XOR 的网络，从两层各 2 个神经元改成隐藏层 8 个神经元（输出层仍 1 个），其他训练设置不变，看能否更快学会。
2. 分别记下「隐藏层 2 个」和「隐藏层 8 个」两种情况下，把误差压到很低大约各需要多少轮训练，比较谁更稳。
3. 把权重初始化时乘的那个小数去掉（改成不缩放），观察训练是否变糟糕。

## 第 58 天 练习题：ReLU vs sigmoid 对比
1. 实现一个"取正数、负数变 0"的激活函数，并写出它"大于 0 处斜率为 1、否则为 0"的导数版本。
2. 把网络的隐藏层激活从之前用的换成这个新函数，其余不变，训练同一份数据。
3. 把网络的隐藏层激活换回原来的函数，同样训练，把两条误差下降曲线画在同一张图上对比，看哪条降得更陡。

## 第 59 天 练习题：权重初始化
1. 把所有权重都设成 0 再训练，观察最后误差是否几乎不动、卡在一个固定值附近。
2. 把权重初始化乘上一个很大的系数（比如 5）再训练，观察是否出现数值异常或乱跳。
3. 把权重初始化乘上一个很小的系数（比如 0.1）再训练，确认它能正常下降。记录这三种情况的现象差异。

## 第 60 天 练习题：mini-batch 与 epoch
1. 给训练流程加上"轮次"概念：每一轮先把所有样本顺序打乱，再切成每批 2 个样本。
2. 对每一个小批单独做"前向→反向→更新权重"，且本批的梯度要除以本批样本数求平均。
3. 训练若干轮后，确认网络仍能学会原来的 4 个样本；并能用一句话说明"一轮"是什么意思。

## 第 61 天 练习题：认识 MNIST 数据
1. 加载手写数字数据集，打印它的样本数量和每张图展开后的特征数量。
2. 把前 10 张图还原成 28×28 的方块画出来，并在每张图下方标上它的正确数字。
3. 把所有像素值从 0–255 的范围缩放到 0–1 之间，并说明为什么训练前要做这步。

## 第 62 天 练习题：把网络升级成 10 分类
1. 实现一个"把一组分数变成概率（每行加起来等于 1）"的输出层函数，计算时注意先减去每行的最大值防止溢出。
2. 实现一个衡量"预测概率分布和真实标签差距"的损失函数，并在里面加一个极小常数防止对 0 取对数。
3. 把网络改成"输入→64 个隐藏单元→10 个输出"，隐藏层用第 58 天的新激活；前向算完后，把标签转成"只有正确位置是 1、其余是 0"的形式，验证前向输出每行加起来约等于 1。
4. 写出输出层的误差应该等于多少（提示：它是 softmax 与交叉熵组合后的简洁结果，等于"预测概率减真实标签"）。

## 第 63 天 练习题：手写 NN 跑通 MNIST
1. 取一部分训练图片和对应标签做训练集、另一部分做测试集；把标签转成"只有正确位置为 1"的形式。
2. 搭一个"784→64→10"的网络，权重用"随机数乘 0.1"、偏置用全 0 初始化；隐藏层用第 58 天的新激活、输出层用第 62 天的输出函数。
3. 训练若干轮，每轮打乱数据并分批更新；把训练集图片和测试集图片都做 0–1 缩放。
4. 训练完后，在测试集上取"概率最大的那一列"作为预测类别，算出预测对的样本比例（即准确率），目标达到 90% 以上。

---

> 📌 **Day 1–63 练习题到此。** 每天小题覆盖当天教程全部知识点，题目只描述功能、不含函数名和代码答案——函数由你自己回想、代码全靠你自己打。做完想核对再看 `Python神经网络每日练习题答案.md`。
> 告诉我「继续写第 10 周」，我就出下一批（PyTorch 入门：张量、自动求导、用框架重写 MNIST）。

---

# 第 10 周练习题（Day 64–70）：PyTorch 基础

> 规则不变：题目只说「要实现什么功能」，函数名你自己回想；每天多小题覆盖当天全部知识点；答案代码不在这里，练完去 `Python神经网络每日练习题答案.md` 核对。

## 第 64 天 练习题：Tensor 基础
1. 创建一个 2 行 2 列的张量（元素用小数），打印它的形状和数据类型。
2. 分别创建全 0、全 1、均匀随机、正态随机（乘 0.1）的 2×3 张量各一个。
3. 把一个 NumPy 数组转换成张量，再把张量转回 NumPy 数组，确认两次转换后值不变。
4. 对这个 2×2 张量做"加 1""逐元素乘 2""矩阵乘自身"三种运算并打印。

## 第 65 天 练习题：自动求导
1. 创建一个要求记录计算过程的标量（初值 2），让它平方后调用反向传播，查看它对原变量的导数是否等于 4。
2. 再试一个"三次项加一次项"的函数，查看在初值 2 处的导数是否等于 38，验证自动求导正确。

## 第 66 天 练习题：用积木搭 XOR 网络
1. 用"线性层"和"激活层"按顺序拼出一个 2→2→1 的网络（隐藏层用平滑激活、输出层用 0~1 激活）。
2. 把这个网络作用到 4 个输入样本上，打印未训练时的输出（应接近 0.5）。
3. 把隐藏层神经元数量从 2 改成 8（即 2→8→1），重新打印网络结构。

## 第 67 天 练习题：损失函数与优化器
1. 生成一组"近似线性关系加噪声"的数据（x 从 0 到 10 取 100 个点）。
2. 用一个"单输入单输出"的线性层当模型，配合均方误差损失和随机梯度下降优化器，训练它逼近这条线性关系。
3. 训练后查看模型学到的斜率和截距是否接近你设定的真实值。

## 第 68 天 练习题：标准训练循环
1. 用"清空梯度 → 前向 → 算损失 → 反向传播 → 更新参数"这五步的固定模板，训练一个 2→2→1 网络解 XOR。
2. 每隔一段轮次打印一次损失，确认它在下降。
3. 训练结束后，把预测结果四舍五入到整数，确认四个样本都预测正确。

## 第 69 天 练习题：DataLoader 与模型保存
1. 把特征和标签打包成一个数据集对象，再用它做一个"自动打乱、每批 2 个"的数据加载器。
2. 用这个加载器喂数据，训练一个解 XOR 的网络。
3. 训练完后把模型权重保存到文件，再从文件加载回一个结构相同的新模型，确认加载后预测仍正确。

## 第 70 天 练习题：复盘——torch 版 vs 手写版
1. 不参考资料，用那五步训练模板自己搭一个 2→4→1 网络（隐藏层 4 个、用非负折叠激活）解 XOR。
2. 画出损失随训练轮次下降的曲线，确认四个样本预测全对。
3. 用一段话说明：用框架时，哪些原本需要手写的部分被"反向传播"和"更新参数"这两行替代了。

---

> 📌 **Day 1–70 练习题到此。** 每天小题覆盖当天教程全部知识点，题目只描述功能、不含函数名和代码答案——函数由你自己回想、代码全靠你自己打。做完想核对再看 `Python神经网络每日练习题答案.md`。
> 告诉我「继续写第 11 周」，我就出下一批（CNN 入门：卷积、池化、LeNet、训练 CNN on MNIST）。

---

# 第 11 周（Day 71–77）练习题

> 题目只描述「要实现什么功能」，函数名由你自己回想，代码全靠你自己打。练完核对见 `Python神经网络每日练习题答案.md`。

## 第 71 天 练习题：卷积的核心操作
1. 用一个 3×3 的小窗口，在 8×8 的数字矩阵上从左到右、从上到下滑动，每个位置做「对应位置相乘再求和」，得到 6×6 的结果矩阵。（这就是卷积的核心操作）
2. 用一组「中间为 8、周围为 −1」的 3×3 窗口去扫一张图，观察输出在物体边缘处和颜色平坦处有什么不同。
3. 把窗口大小改成 5×5，重新算输出尺寸，验证它变成 4×4。

## 第 72 天 练习题：卷积层参数
1. 给定输入 28×28、窗口 3×3、四周各补 1 圈 0、步长 1，手算出输出尺寸并验证等于 28。
2. 把步长改成 2，重新算输出尺寸（应约为 14）。
3. 用框架的二维卷积层（输入 1 通道、输出 4 通道、窗口 3、补 1、步长 1）处理一张「1 张图、1 通道、28×28」的批量数据，确认输出形状是 4 通道 28×28。

## 第 73 天 练习题：搭一个 LeNet 风格小网络
1. 在一个「1 张图、1 通道、32×32」的假数据上，搭一条「卷积 → 激活 → 池化 → 卷积 → 激活 → 池化 → 展平 → 两层全连接」的网络，打印每一层输出的形状。
2. 数一数第一层卷积有多少个可学习参数（窗口大小 × 输入通道 × 输出通道 + 输出通道个偏置）。
3. 把假图尺寸从 32×32 改成 64×64，重新跑一遍，观察每层形状怎么变。

## 第 74 天 练习题：训练 CNN on MNIST
1. 把之前训练 MNIST 的全连接模型换成卷积网络（卷积 + 池化 + 全连接），训练 1 个轮次，记录测试准确率。
2. 和之前全连接模型同一轮次的准确率对比，说明卷积网络是否更高。
3. 训练前确认图片有「通道」这一维（28×28 的图要变成 1×28×28），否则卷积层会报错。

## 第 75 天 练习题：可视化卷积核
1. 取出训练好的卷积网络第一层权重，把每个「输出通道对应的小窗口」画成一张小图，排成网格。
2. 画图前先把权重值缩放到 0–1 之间（减去最小值再除以最大值与最小值之差），避免图全黑或全白。

## 第 76 天 练习题：CNN 在车辆上的应用（选做）
1. 加载一个公开的预训练图像分类模型，对一张本地图片做推理，打印最可能的 5 个类别。
2. 思考并写下：要把这个模型用于「识别 43 类交通标志」，需要改哪里（提示：输出类别数）。
3. 说明预训练模型对输入图片的尺寸和数值范围通常有什么固定要求。

## 第 77 天 练习题：复盘——画 CNN 结构图
1. 在纸上画出一条数据流：输入 1×28×28 → 卷积(8 通道) → 池化 → 卷积(16 通道) → 池化 → 展平 → 全连接(64) → 全连接(10)，并标注每层输出尺寸。
2. 不借助代码，纯手算验证第 73 天网络在 64×64 输入时每一层的尺寸。

---

> 📌 **Day 1–77 练习题到此。** 每天小题覆盖当天教程全部知识点，题目只描述功能、不含函数名和代码答案——函数由你自己回想、代码全靠你自己打。做完想核对再看 `Python神经网络每日练习题答案.md`。
> 告诉我「继续写第 12 周」，我就出下一批（训练技巧：正则化/Dropout/早停 + 毕业项目 GTSRB）。

---

# 第 12 周（Day 78–84）练习题

> 题目只描述「要实现什么功能」，函数名由你自己回想，代码全靠你自己打。练完核对见 `Python神经网络每日练习题答案.md`。这是最后一周。

## 第 78 天 练习题：训练技巧
1. 给之前训练的卷积网络在两个全连接层之间加一个"训练时随机丢弃部分输出"的层（丢弃概率 0.2），重训 3 个轮次，对比加之前后的测试准确率。
2. 说明：为什么这种随机丢弃能防止过拟合（提示：强迫网络不依赖单个节点）。
3. 确认在评估模型时切到了"评估模式"，否则随机丢弃层仍会生效，导致结果不稳定。

## 第 79 天 练习题：调参思路
1. 对卷积网络做一组小网格实验：学习率取两个档位（较大/较小）× 批量大小取两个档位（较大/较小），各跑 2 个轮次，记录 4 组测试准确率。
2. 把 4 组结果列成表格，指出哪一组最好，并解释原因。
3. 每组实验前固定随机种子，保证结果可比较。

## 第 80 天 练习题：学习率调度
1. 给优化器包一个"每 2 个轮次把学习率减半"的调度器，每个轮次结束后调用一次它的更新。
2. 画出加了调度器后的训练损失曲线，对比固定学习率版本，说明后期为什么更稳。

## 第 81 天 练习题：毕业项目启动——交通标志数据集
1. 查看交通标志数据集的目录结构，确认一共有多少个类别。
2. 统计每个类别的样本数量，画一张各类别样本数的分布图，观察是否均衡。

## 第 82 天 练习题：数据加载与预处理
1. 写一个能读取"图片路径 + 类别标签"的数据集类，配合批量加载器使用。
2. 把每张图统一缩放成 32×32，转成张量，并做标准化（减均值除标准差）。
3. 从加载器里取出一个批次（8 张图），把图片和对应类别名一起显示出来，检查数据管道是否正确。

## 第 83 天 练习题：训练与评估
1. 把第 11 周卷积网络的第一层输入通道改成 3（彩色），最后一层输出改成 43（类别数），套用训练模板训练几个轮次，记录测试准确率。
2. 画一张混淆矩阵，找出最容易认错的两类（如限速 30 和 50）。
3. 说明：当各类别样本数不均衡时，单看总准确率可能误导，为什么混淆矩阵更重要。

## 第 84 天 练习题：总复盘
1. 翻看 12 周笔记，列出你最自豪的 3 个成果。
2. 写一段约 300 字《我现在理解的神经网络》总结。
3. 给下一阶段定一个方向（如目标检测、车道线分割、或 Transformer）。

---

> 🎊 **Day 1–84 练习题全部完成。** 这是整个 84 天计划的最后一周练习题。做完这 7 天，你就走完了从"二级 Python"到"能训练自己交通标志分类模型"的完整路径。核对答案见 `Python神经网络每日练习题答案.md`。
