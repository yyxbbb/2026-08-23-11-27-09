# Python → 神经网络：12 周每日 20 分钟学习计划

> 适用对象：智能车辆工程专业大三，Python 水平为计算机二级（掌握基础语法、简单库使用）  
> 最终目标：**亲手用 NumPy 写出一个神经网络，看懂前向传播 / 反向传播 / 梯度下降，并会用 PyTorch 训练模型**

---

## 一、总体安排

| 项目   | 说明                                               |
| ---- | ------------------------------------------------ |
| 每日时长 | 20 分钟 = 3 分钟回顾昨天 + 8 分钟理论 + 9 分钟动手实践             |
| 总周期  | 12 周（84 天），每周第 7 天为复盘日（不学新内容）                    |
| 核心原则 | **先手搓、后框架**：所有核心算法先用 NumPy 手写一遍，最后 3 周才碰 PyTorch |
| 学习方式 | 理论看视频/文档，实践必须自己敲代码（不许复制粘贴）                       |

### 进度总览

| 阶段 | 周数        | 主题                   | 阶段产出（里程碑）                 |
| -- | --------- | -------------------- | ------------------------- |
| 一  | 第 1–2 周   | Python 科学计算基础（NumPy） | 能熟练用矩阵运算代替 for 循环         |
| 二  | 第 3–4 周   | 数学基础（向量、导数、梯度）       | 用代码实现梯度下降求函数最小值           |
| 三  | 第 5–6 周   | 机器学习入门（回归与分类）        | 手写线性回归 + 逻辑回归并画图          |
| 四  | 第 7–9 周   | **神经网络核心（重点）**       | 手写反向传播解 XOR；手写 NN 跑 MNIST |
| 五  | 第 10–12 周 | PyTorch 框架 + CNN 入门  | 用 PyTorch 训练 CNN 完成一个小项目  |

---

## 二、环境准备（开始前某天空闲时一次性完成，不占每天 20 分钟）

1. 安装 Miniconda 到 **E:\miniconda3**（不要装 C 盘）。
2. 创建环境并安装依赖（在 Anaconda Prompt 中执行）：
   ```
   conda create -n dl python=3.11 -y
   conda activate dl
   pip install numpy matplotlib scikit-learn torch torchvision -i https://pypi.tuna.tsinghua.edu.cn/simple
   ```

3. 安装 VS Code + Jupyter 插件（或直接用 Jupyter Notebook）。
4. 建一个代码文件夹，如 `E:\workbuddy1\python-nn-study\`，每周一个子文件夹（week01 ~ week12）。

**推荐资源（配合使用，不要求全看）：**

- 视频：B 站搜「3Blue1Brown 神经网络」（第 4 阶段前必看，4 集共约 1 小时）
- 文档：《动手学深度学习》（d2l.ai，中文免费，选读）
- 查资料：MDN/菜鸟教程查语法，遇到不懂的概念优先搜「XX 可视化」

---

## 三、详细计划

### 阶段一：Python 科学计算基础（第 1–2 周）

> 目标：神经网络的所有计算都是矩阵运算，这两周把 NumPy 练熟。

**第 1 周**

| 天 | 理论（8 分钟）                                                                    | 实践（9 分钟）                                                      |
| - | --------------------------------------------------------------------------- | ------------------------------------------------------------- |
| 1 | NumPy 安装与 ndarray：`array / arange / zeros / ones`，属性 `shape / dtype / ndim` | 创建一个 3×3 矩阵，打印其 shape、dtype；用 `reshape` 把 `arange(12)` 变成 3×4 |
| 2 | 索引与切片：布尔索引、花式索引 `a[a>5]`                                                    | 生成 5×5 随机矩阵，取出其中所有大于 0.5 的元素并计数                               |
| 3 | 广播机制 Broadcasting：形状不同的数组如何运算                                               | 用广播把形状 (3,) 的向量加到 (3,4) 矩阵的每一列；再试试会报错的反例                      |
| 4 | 矩阵运算：`dot / @ / T / sum(axis=0或1)`                                          | 验证 `A@B`（矩阵乘）与 `A*B`（逐元素乘）的区别，手算一个 2×2 例子对照                   |
| 5 | 随机与统计：`randn / rand / seed / mean / std`                                    | 固定 seed 生成 1000 个正态样本，打印均值和标准差；换 seed 再看一次                    |
| 6 | matplotlib 入门：`plot / scatter / xlabel / show`                              | 画 y=sin(x) 折线图 + 30 个随机散点在同一张图                                |
| 7 | **复盘日**                                                                     | 不看资料，重写第 3、4、6 天的代码；写 3 句话总结本周                                |

**第 2 周**

| 天  | 理论（8 分钟）                                 | 实践（9 分钟）                                                                      |
| -- | ---------------------------------------- | ----------------------------------------------------------------------------- |
| 8  | 函数进阶：默认参数、`*args/**kwargs`、lambda        | 把第 1 周的随机矩阵代码封装成函数 `make_matrix(rows, cols, seed)`                            |
| 9  | 面向对象：`class`、`__init__`、实例方法             | 写一个 `Linear` 类：`__init__` 里存权重 W 和偏置 b（用 `randn` 初始化）                         |
| 10 | 类进阶：继承、`__call__`、实例属性                   | 给 `Linear` 加 `forward(x)` 方法返回 `x @ W + b`，跑一下验证形状                            |
| 11 | 异常处理与文件：`try/except`、`np.save / np.load` | 保存一个数组到文件再读回来，对比是否一致                                                          |
| 12 | Jupyter Notebook 使用技巧：单元格、Markdown 笔记    | 把本周代码整理成一个 notebook，每个函数配一行 Markdown 说明                                       |
| 13 | 综合练习日                                    | 用 NumPy 实现两个函数：`euclidean_dist(a,b)`（欧氏距离）和 `cosine_sim(a,b)`（余弦相似度），各用两组向量测试 |
| 14 | **复盘日**                                  | 自测：广播规则是什么？`@` 和 `*` 区别？写不出来就翻笔记本                                             |

---

### 阶段二：数学基础（第 3–4 周）

> 目标：只需要「够用」的高数和线代直觉，全部用代码验证，不抠证明。

**第 3 周**

| 天  | 理论（8 分钟）            | 实践（9 分钟）                                                 |
| -- | ------------------- | -------------------------------------------------------- |
| 15 | 向量点积的几何意义（投影、相似度）   | NumPy 验证 `a@b == b@a`；用余弦相似度比较 3 个向量的方向                  |
| 16 | 矩阵乘法 = 线性变换（旋转/缩放）  | 用 2×2 矩阵 `[[0,-1],[1,0]]` 作用在点 (1,0) 上，matplotlib 画出变换前后 |
| 17 | 导数复习：导数是斜率、是变化率     | 用数值导数 `(f(x+h)-f(x))/h` 验证 f(x)=x² 在 x=3 处导数≈6           |
| 18 | 偏导数与梯度：梯度指向上升最快方向   | 数值法求 f(x,y)=x²+y² 在 (1,2) 处的梯度，与解析解 (2,4) 对照             |
| 19 | 链式法则：复合函数求导         | 验证 f(g(x))，g=x²，f=x+1，数值导数 vs 解析导数 2x                    |
| 20 | **梯度下降法**：沿负梯度方向小步走 | 用梯度下降求 f(x)=x²−4x 的最小值，画出 x 的迭代轨迹图                       |
| 21 | **复盘日**             | 合上资料，从零重写「梯度下降求最小值」的 10 行代码                              |

**第 4 周**

| 天  | 理论（8 分钟）                 | 实践（9 分钟）                                                        |
| -- | ------------------------ | --------------------------------------------------------------- |
| 22 | 损失函数概念：衡量「预测有多差」         | 用 NumPy 实现 `mse(y_pred, y_true)`，用两组数测试                         |
| 23 | 激活函数：为什么需要非线性；sigmoid 公式 | 画出 sigmoid 曲线（−6 到 6）；再画 tanh 和 ReLU 曲线，三张子图对比                  |
| 24 | sigmoid 求导的性质 σ′=σ(1−σ)  | 用数值导数在 5 个点上验证该公式                                               |
| 25 | 概率基础：期望、方差、正态分布          | 抽 10000 个正态样本画直方图（`hist`），叠加理论曲线（选做）                            |
| 26 | 交叉熵直觉：衡量两个概率分布的差异        | 实现二元交叉熵 `−[y·log(p)+(1−y)·log(1−p)]`，用 p=0.9/y=1 和 p=0.1/y=1 对比 |
| 27 | 综合复习日                    | 3 道自测题：①手推 x³ 在 x=2 的导数并用代码验证 ②实现 tanh 的数值导数 ③解释学习率太大/太小会怎样     |
| 28 | **阶段复盘**                 | 写 200 字总结：「训练一个模型需要哪些零件？」（数据、模型、损失、梯度、更新规则）                     |

---

### 阶段三：机器学习入门（第 5–6 周）

> 目标：从零手写完整训练流程「生成数据 → 定义模型 → 算损失 → 梯度下降 → 可视化」，这是神经网络的直接前置。

**第 5 周**

| 天  | 理论（8 分钟）                | 实践（9 分钟）                                                        |
| -- | ----------------------- | --------------------------------------------------------------- |
| 29 | 什么是机器学习：监督/无监督；训练=找参数   | 用 `sklearn.datasets.load_iris()` 加载鸢尾花数据，打印 `data.shape`，看前 5 行 |
| 30 | 线性回归模型：ŷ = wx + b       | 生成数据：x 在 0~10 均匀取 100 个点，y = 2x + 1 + 噪声，画散点图                   |
| 31 | 损失对参数的梯度（w、b 各自怎么更新）    | 手写梯度下降训练线性回归，迭代 100 次，打印每次 loss                                 |
| 32 | **学习率**：步子大小的 trade-off | 分别用 lr=0.001 / 0.01 / 1.0 训练，把三条 loss 曲线画在一张图对比                 |
| 33 | 收敛判断与早停                 | 给训练加条件：loss 下降小于 1e-6 就停止；记录迭代次数                                |
| 34 | 向量化：用矩阵一次算全部样本          | 把第 31 天的 for 循环版本改成 `X @ w` 向量化版本，用 `%timeit` 对比速度              |
| 35 | **复盘日**                 | 画出最终拟合直线盖在散点上；检查学到的 w、b 是否接近 2 和 1                              |

**第 6 周**

| 天  | 理论（8 分钟）                 | 实践（9 分钟）                                                                     |
| -- | ------------------------ | ---------------------------------------------------------------------------- |
| 36 | 过拟合/欠拟合；训练集与测试集          | 手写 `split` 把数据 8:2 分开；用高次多项式拟合少量点，观察过拟合（可用 `sklearn` 的 `PolynomialFeatures`） |
| 37 | 分类问题：逻辑回归 = 线性 + sigmoid | 生成两类二维散点（各 50 个，中心不同）；实现 `predict(x) = sigmoid(w@x+b)` 前向                    |
| 38 | 用交叉熵训练逻辑回归               | 在两类散点上梯度下降训练，loss 下降后画决策边界直线                                                 |
| 39 | 评估：准确率、混淆矩阵              | 统计训练后分类准确率；打印 2×2 混淆矩阵（TP/FP/FN/TN）                                          |
| 40 | 特征标准化：为什么以及怎么做           | 实现 z-score 标准化 `(x-mean)/std`，对比标准化前后的 loss 收敛速度                             |
| 41 | 综合实战日                    | 串成完整 pipeline：生成数据 → 划分 → 标准化 → 训练 → 评估 → 画图，全程一个函数搞定                        |
| 42 | **阶段复盘**                 | 自测：能否默写「训练五步曲」？写不出来回看第 28 天总结                                                |

---

### 阶段四：神经网络核心（第 7–9 周）⭐ 全计划最重要的三周

> 目标：亲手实现多层感知机 + 反向传播。看 3Blue1Brown 神经网络系列（每周看 1 集，当成理论时间）。

**第 7 周：前向传播**

| 天  | 理论（8 分钟）               | 实践（9 分钟）                                                                       |
| -- | ---------------------- | ------------------------------------------------------------------------------ |
| 43 | 感知机：加权求和 + 阈值激活        | 手写 AND 门感知机（w=[0.5,0.5], b=−0.7），验证 4 种输入                                      |
| 44 | 单层感知机的局限：XOR 不可分       | 用随机权重训练单层感知机解 XOR，观察它永远失败，感受「线性不可分」                                            |
| 45 | 多层感知机结构：隐藏层解决 XOR      | 在纸上画 2−2−1 网络，数一数参数个数（2×2+2 + 2×1+1 = 9 个）                                     |
| 46 | 前向传播：逐层计算              | NumPy 实现 2−2−1 网络前向（隐藏层 tanh，输出层 sigmoid），随机权重跑一次                              |
| 47 | 前向传播的矩阵形式（一次算一个 batch） | 把输入改成 4×2（XOR 全部四种），一次前向算出 4 个输出                                               |
| 48 | 把网络封装成类                | 用第 2 周的 class 知识写 `NeuralNetwork` 类：`__init__` 存 W1,b1,W2,b2，`forward(x)` 输出结果 |
| 49 | **复盘日**                | 画出（纸上或 matplotlib）前向传播计算图，标出每个中间量的形状                                           |

**第 8 周：反向传播（最难也最关键，纸笔推导 + 代码）**

| 天  | 理论（8 分钟）                  | 实践（9 分钟）                                                   |
| -- | ------------------------- | ---------------------------------------------------------- |
| 50 | 计算图思想：前向算值，反向传导数          | 纸上推导：f(x)= (x+1)²，用链式法则求 df/dx，再用代码验证                      |
| 51 | 输出层误差公式（MSE + sigmoid 情形） | 纸上推 δ_L = (ŷ−y)·σ′(z)；对照 3Blue1Brown 第 3 集理解               |
| 52 | 误差逐层回传：δ 与上一层的关系          | 纸上推 δ 隐藏层 = (W₂ᵀδ_L)·tanh′(z)；不要求会背，要求能看懂                  |
| 53 | 权重梯度 = 本层输入 × 上游误差        | 代码：实现 2−2−1 网络的 `backward()`，先只算梯度不更新                      |
| 54 | 梯度校验（gradient check）      | 用数值梯度 `dW ≈ (L(W+ε)−L(W−ε))/(2ε)` 验证手写反传梯度，误差应 < 1e−6      |
| 55 | 拼成完整训练循环                  | 前向 → 算 loss → 反向 → 更新；训练 XOR，画 loss 曲线                     |
| 56 | **里程碑：XOR 训练成功**          | 调整训练轮数/学习率直到 4 个输入全部分对，loss < 0.01；庆祝一下，你已经理解了 1986 年的深度学习 |

**第 9 周：从玩具到真实数据**

| 天  | 理论（8 分钟）                   | 实践（9 分钟）                                                            |
| -- | -------------------------- | ------------------------------------------------------------------- |
| 57 | 隐藏层宽度/深度的影响                | 把 XOR 网络改成 2−4−1、2−8−1，观察收敛速度差异                                     |
| 58 | 激活函数对比：ReLU vs sigmoid     | 同一网络分别用 sigmoid/ReLU 隐藏层，对比 loss 曲线                                 |
| 59 | 权重初始化的影响                   | 把 W 初始化为全 0 再训练，观察网络「学不动」的现象，解释原因                                   |
| 60 | mini-batch 训练与 epoch 概念    | 给训练加 batch：每轮打乱数据、分批更新（batch=8）                                     |
| 61 | MNIST 数据集认识：28×28 灰度图、10 类 | 用 `sklearn.datasets.fetch_openml` 或 torchvision 加载 MNIST，可视化前 10 张图 |
| 62 | 把网络升级为 784−64−10           | 改造 `NeuralNetwork` 类（输出层 softmax + 交叉熵），开始训练 MNIST                  |
| 63 | **里程碑：手写 NN 跑通 MNIST**     | 在测试集上算准确率（目标 ≥ 90%）；复盘：纯手写代码完成到此为止                                  |

---

### 阶段五：PyTorch 框架与进阶（第 10–12 周）

> 目标：把手写的轮子换成工业级框架，并入门 CNN（智能车辆感知的基础）。

**第 10 周：PyTorch 基础**

| 天  | 理论（8 分钟）                  | 实践（9 分钟）                                                                          |
| -- | ------------------------- | --------------------------------------------------------------------------------- |
| 64 | 为什么用框架：自动求导、GPU、生态        | Tensor 基础：创建、运算、`torch.from_numpy` 与 `tensor.numpy()` 互转                          |
| 65 | autograd：自动微分机制           | `x=torch.tensor(2., requires_grad=True)`；对 y=x² 做 `backward()`，查看 `x.grad` 是否等于 4 |
| 66 | `nn.Module` 与 `nn.Linear` | 用 `nn.Sequential(nn.Linear(2,2), nn.Tanh(), nn.Linear(2,1))` 搭出 XOR 网络            |
| 67 | 损失函数与优化器                  | 用 torch 重写第 5 周的线性回归：`MSELoss` + `SGD`，对比与手写版代码量                                  |
| 68 | 标准训练循环模板                  | 5 行模板：`optimizer.zero_grad() → loss.backward() → optimizer.step()`，背下来            |
| 69 | DataLoader 与模型保存          | 用 `DataLoader` 包 MNIST；训练后 `torch.save(model.state_dict())` 再 load 回来验证           |
| 70 | **复盘日**                   | 用 torch 版网络解 XOR，与第 56 天手写版对比：哪些代码消失了？                                            |

**第 11 周：CNN 入门（与智能车辆最相关）**

| 天  | 理论（8 分钟）                    | 实践（9 分钟）                                                     |
| -- | --------------------------- | ------------------------------------------------------------ |
| 71 | 卷积：局部感受野、权值共享               | 手写双重 for 循环用 3×3 卷积核扫一张 8×8 图，输出 6×6（再用边缘检测核看效果）             |
| 72 | 卷积层参数：stride / padding / 通道 | 手算输出尺寸公式 (W−K+2P)/S+1；用 `nn.Conv2d` 传不同参数验证                  |
| 73 | 池化与 CNN 整体结构（LeNet）         | 搭一个 LeNet 风格小网络：Conv→Pool→Conv→Pool→FC，打印每层输出形状              |
| 74 | **训练 CNN on MNIST**         | 训练 1 个 epoch，对比与第 63 天 MLP 的准确率（应提升 1–3%）                    |
| 75 | 可视化：卷积核长什么样                 | 取出第一层卷积核权重画成 3×3 小图；可视化某张图的特征图（选做）                           |
| 76 | CNN 在车辆上的应用概念               | 了解车道线检测 / 交通标志识别 / 目标检测都是 CNN；跑一个 torchvision 预训练模型推理一张图（选做） |
| 77 | **复盘日**                     | 画一张 CNN 结构图（输入→卷积→池化→全连接→输出），标注每层尺寸                          |

**第 12 周：整合与毕业项目**

| 天  | 理论（8 分钟）            | 实践（9 分钟）                                                |
| -- | ------------------- | ------------------------------------------------------- |
| 78 | 训练技巧：正则化、Dropout、早停 | 给第 74 天的 CNN 加 `nn.Dropout(0.2)` 重训，对比准确率               |
| 79 | 超参数与调参思路            | 做小网格实验：lr ∈ {0.01, 0.001} × batch ∈ {32, 128}，记录 4 组结果  |
| 80 | 学习率调度               | 加 `StepLR` 每 2 个 epoch 衰减一半，观察 loss 曲线变化                |
| 81 | 毕业项目启动：交通标志分类       | 下载 GTSRB 数据集（德国交通标志，43 类；或任选 Kaggle 上的小型车辆数据集），看数据结构    |
| 82 | 项目：数据加载与预处理         | 写 `Dataset/DataLoader`，做 resize 到 32×32、标准化，可视化一个 batch |
| 83 | 项目：训练与评估            | 用第 11 周的 CNN 训练，输出测试集准确率和混淆矩阵，找出最容易认错的类别                |
| 84 | **总复盘**             | 回顾 12 周笔记；写 300 字总结《我现在理解的神经网络》；制定下一阶段方向（见下文）           |

---

## 四、里程碑自检清单

- [ ] 第 2 周末：能用 NumPy 矩阵运算替代 for 循环
- [ ] 第 4 周末：能用梯度下降求函数最小值并画出轨迹
- [ ] 第 6 周末：能默写「训练五步曲」并手写线性回归 + 逻辑回归
- [ ] 第 8 周末：手写反向传播解决 XOR（本计划最关键里程碑）
- [ ] 第 9 周末：手写神经网络在 MNIST 上达到 90%+ 准确率
- [ ] 第 12 周末：用 PyTorch 训练 CNN 完成一个图像分类小项目

## 五、坚持的技巧

1. **固定时间**：绑定一个已有习惯（如晚饭后立刻坐下），20 分钟一到就停，保持「意犹未尽」。
2. **代码入库**：每天代码存入 `E:\workbuddy1\python-nn-study\weekXX\`，复盘日翻看进步。
3. **卡住 5 分钟就跳过**：先标记，复盘日集中解决；反向传播推导允许「先跑通代码，再回头补推导」。
4. **缺勤规则**：漏一天直接继续下一天，不补课不内疚；漏三天以上则当周复盘日重做本周实践。

## 六、12 周之后的方向（与智能车辆工程结合）

- **感知方向**：目标检测（YOLO）、语义分割（车道线检测的基础）
- **序列方向**：RNN/LSTM（轨迹预测）
- **进阶理论**：Transformer、注意力机制
- **实践平台**：Kaggle 入门赛、CARLA 仿真器里跑感知模块
